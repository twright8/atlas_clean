{"id":"modules/interconnectionChart.js","dependencies":[{"name":"C:\\Users\\TomWright\\PycharmProjects\\atlas_clean\\package.json","includedInParent":true,"mtime":1762510291638},{"name":"d3","loc":{"line":5,"column":20,"index":197},"parent":"C:\\Users\\TomWright\\PycharmProjects\\atlas_clean\\src\\modules\\interconnectionChart.js","resolved":"C:\\Users\\TomWright\\PycharmProjects\\atlas_clean\\node_modules\\d3\\src\\index.js"}],"generated":{"js":"\"use strict\";\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nexports.default = void 0;\nexports.initializeInterconnectionChart = initializeInterconnectionChart;\nexports.updateInterconnectionChart = updateInterconnectionChart;\nvar d3 = _interopRequireWildcard(require(\"d3\"));\nfunction _getRequireWildcardCache(e) { if (\"function\" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function (e) { return e ? t : r; })(e); }\nfunction _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || \"object\" != typeof e && \"function\" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if (\"default\" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }\nfunction _nonIterableSpread() { throw new TypeError(\"Invalid attempt to spread non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.\"); }\nfunction _iterableToArray(r) { if (\"undefined\" != typeof Symbol && null != r[Symbol.iterator] || null != r[\"@@iterator\"]) return Array.from(r); }\nfunction _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }\nfunction _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }\nfunction _nonIterableRest() { throw new TypeError(\"Invalid attempt to destructure non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.\"); }\nfunction _unsupportedIterableToArray(r, a) { if (r) { if (\"string\" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return \"Object\" === t && r.constructor && (t = r.constructor.name), \"Map\" === t || \"Set\" === t ? Array.from(r) : \"Arguments\" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }\nfunction _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }\nfunction _iterableToArrayLimit(r, l) { var t = null == r ? null : \"undefined\" != typeof Symbol && r[Symbol.iterator] || r[\"@@iterator\"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }\nfunction _arrayWithHoles(r) { if (Array.isArray(r)) return r; } /**\r\n * Module for the advanced category interconnection visualization (Fixed Radial Layout)\r\n * Uses specific translate values for visual centering in dashboard context.\r\n */\n/**\r\n * Initialize the categories interconnection chart\r\n * @returns {Object} The chart reference (the main <g> element)\r\n */\nfunction initializeInterconnectionChart() {\n  var container = d3.select('#interconnection-chart');\n  container.select('svg').remove(); // Clear previous SVG\n\n  var margin = {\n    top: 40,\n    right: 40,\n    bottom: 40,\n    left: 40\n  };\n  var containerElement = document.getElementById('interconnection-chart');\n  var containerWidth = containerElement ? containerElement.clientWidth : 700;\n  var containerHeight = 400;\n  // Calculate drawing area dimensions\n  var width = Math.max(containerWidth - margin.left - margin.right, 100);\n  var height = Math.max(containerHeight - margin.top - margin.bottom, 100);\n  var svg = container.append('svg').attr('width', width + margin.left + margin.right).attr('height', height + margin.top + margin.bottom).append('g')\n  // *** RESTORED: Translate figures as specified by user for visual centering ***\n  // This positions the origin (0,0) of the chart group based on visual preference\n  // rather than standard D3 drawing area centering.\n  .attr('transform', \"translate(\".concat(margin.left + width * 2, \",\").concat(margin.top + height / 3, \")\"));\n  svg.append('g').attr('class', 'links');\n  svg.append('g').attr('class', 'nodes'); // Nodes group to contain circles and labels\n  svg.append('g').attr('class', 'labels'); // For overall chart labels/legends\n  svg.append('defs');\n\n  // Store drawing area dimensions for update function\n  svg.node()._chartWidth = width;\n  svg.node()._chartHeight = height;\n  return svg;\n}\n\n/**\r\n * Update the categories interconnection chart (Fixed Radial Layout)\r\n * Creates an interactive fixed radial bipartite graph showing category connections.\r\n * Tooltip is pinned to the bottom right and truncates long category names.\r\n * Uses large radius corresponding to the specific translate for visual layout.\r\n * @param {Object} chart The chart reference (the main <g> element from initialize)\r\n * @param {Array} data Filtered data array\r\n */\nfunction updateInterconnectionChart(chart, data) {\n  console.log(\"Updating interconnection chart with \".concat(data.length, \" data points (Fixed Radial, Custom Center)\"));\n  var width = chart.node()._chartWidth;\n  var height = chart.node()._chartHeight;\n  // *** RESTORED: Large radius multiplier as preferred by user ***\n  var radius = Math.min(width, height) * 1.2;\n\n  // Clear previous elements\n  chart.select('.links').selectAll('*').remove();\n  chart.select('.nodes').selectAll('*').remove(); // Clear nodes group specifically\n  chart.select('.labels').selectAll('*').remove();\n  chart.select('defs').selectAll('*').remove();\n\n  // --- Tooltip Setup (Pinned via CSS) ---\n  var tooltipContainer = d3.select('#interconnection-chart');\n  tooltipContainer.selectAll('.chart-tooltip-interconnect').remove();\n  var tooltip = tooltipContainer.append('div').attr('class', 'chart-tooltip-interconnect').style('opacity', 0);\n  if (!data || data.length === 0) {\n    // No data handling\n    chart.select('.labels').append('text') // Add message to labels group\n    .attr('class', 'no-data-message').attr('x', 0).attr('y', 0).attr('text-anchor', 'middle') // Centered relative to the (translated) group origin\n    .style('font-size', '14px').style('fill', '#666').text('No data available');\n    return;\n  }\n\n  // --- Data Processing --- (Same as before)\n  var corruptionCategories = new Map();\n  var healthCategories = new Map();\n  data.forEach(function (item) {\n    if (Array.isArray(item['Corruption Categories'])) {\n      item['Corruption Categories'].forEach(function (cat) {\n        if (cat !== null && cat !== void 0 && cat.trim()) corruptionCategories.set(cat.trim(), (corruptionCategories.get(cat.trim()) || 0) + 1);\n      });\n    }\n    if (Array.isArray(item['Sector Categories'])) {\n      item['Sector Categories'].forEach(function (cat) {\n        if (cat !== null && cat !== void 0 && cat.trim()) healthCategories.set(cat.trim(), (healthCategories.get(cat.trim()) || 0) + 1);\n      });\n    }\n  });\n  var integrityNodes = Array.from(corruptionCategories.entries()).map(function (_ref) {\n    var _ref2 = _slicedToArray(_ref, 2),\n      name = _ref2[0],\n      count = _ref2[1];\n    return {\n      id: name,\n      group: 'integrity',\n      count: count\n    };\n  });\n  var healthNodes = Array.from(healthCategories.entries()).map(function (_ref3) {\n    var _ref4 = _slicedToArray(_ref3, 2),\n      name = _ref4[0],\n      count = _ref4[1];\n    return {\n      id: name,\n      group: 'health',\n      count: count\n    };\n  });\n  var allNodes = [].concat(_toConsumableArray(integrityNodes), _toConsumableArray(healthNodes));\n  var links = [];\n  integrityNodes.forEach(function (integrity) {\n    healthNodes.forEach(function (health) {\n      var coOccurrenceCount = 0;\n      data.forEach(function (item) {\n        var hasIntegrity = Array.isArray(item['Corruption Categories']) && item['Corruption Categories'].includes(integrity.id);\n        var hasHealth = Array.isArray(item['Sector Categories']) && item['Sector Categories'].includes(health.id);\n        if (hasIntegrity && hasHealth) coOccurrenceCount++;\n      });\n      if (coOccurrenceCount > 0) {\n        links.push({\n          source: integrity.id,\n          target: health.id,\n          value: coOccurrenceCount\n        });\n      }\n    });\n  });\n  if (links.length === 0 && allNodes.length > 0) {\n    /* ... no connections message ... */\n    chart.select('.labels').append('text').attr('class', 'no-connections-message').attr('x', 0).attr('y', 0).attr('text-anchor', 'middle').style('font-size', '14px').style('fill', '#666').text('No connections found');\n  }\n  if (allNodes.length === 0) {\n    /* ... no categories message ... */\n    chart.select('.labels').append('text').attr('class', 'no-data-message').attr('x', 0).attr('y', 0).attr('text-anchor', 'middle').style('font-size', '14px').style('fill', '#666').text('No categories found');\n    return;\n  }\n\n  // --- Visualization Setup ---\n  var color = d3.scaleOrdinal().domain(['integrity', 'health']).range(['#3694d1', '#e5007d']);\n  var maxNodeCount = d3.max(allNodes, function (d) {\n    return d.count;\n  }) || 1;\n  var nodeSize = d3.scaleSqrt().domain([0, maxNodeCount]).range([3, 12]);\n  var maxLinkValue = d3.max(links, function (d) {\n    return d.value;\n  }) || 1;\n  var linkWidth = d3.scaleLinear().domain([0, maxLinkValue]).range([1, 7]);\n\n  // --- Force Simulation (Collision/Charge Only) ---\n  var simulation = d3.forceSimulation(allNodes).force('link', d3.forceLink(links).id(function (d) {\n    return d.id;\n  }).strength(0.01)).force('charge', d3.forceManyBody().strength(-50)).force('collide', d3.forceCollide(function (d) {\n    return nodeSize(d.count) + 15;\n  }).strength(0.9));\n\n  // --- Drawing Elements ---\n  var defs = chart.select('defs');\n  var linkGroup = chart.select('.links');\n  var nodeGroup = chart.select('.nodes'); // Select the dedicated nodes group\n\n  var link = linkGroup.selectAll('path').data(links).enter().append('path').attr('class', 'link').style('stroke-width', function (d) {\n    return linkWidth(d.value);\n  }).style('stroke-opacity', 0.5).style('fill', 'none').style('stroke', function (d) {\n    var _allNodes$find, _allNodes$find2;\n    /* Gradient logic */\n    // Ensure source/target objects exist if link data uses them\n    var sourceGroup = d.source.group || ((_allNodes$find = allNodes.find(function (n) {\n      return n.id === d.source;\n    })) === null || _allNodes$find === void 0 ? void 0 : _allNodes$find.group);\n    var targetGroup = d.target.group || ((_allNodes$find2 = allNodes.find(function (n) {\n      return n.id === d.target;\n    })) === null || _allNodes$find2 === void 0 ? void 0 : _allNodes$find2.group);\n    if (!sourceGroup || !targetGroup) return '#ccc'; // Fallback if group info is missing\n\n    var sourceIdClean = String(d.source.id || d.source).replace(/[^a-zA-Z0-9-_]/g, '_');\n    var targetIdClean = String(d.target.id || d.target).replace(/[^a-zA-Z0-9-_]/g, '_');\n    var gradientId = \"link-gradient-\".concat(sourceIdClean, \"-\").concat(targetIdClean);\n    if (defs.select(\"#\".concat(gradientId)).empty()) {\n      var gradient = defs.append('linearGradient').attr('id', gradientId).attr('gradientUnits', 'userSpaceOnUse');\n      gradient.append('stop').attr('offset', '0%').attr('stop-color', color(sourceGroup));\n      gradient.append('stop').attr('offset', '100%').attr('stop-color', color(targetGroup));\n    }\n    return \"url(#\".concat(gradientId, \")\");\n  });\n\n  // Create node groups\n  var node = nodeGroup.selectAll('g.node').data(allNodes, function (d) {\n    return d.id;\n  }).enter().append('g').attr('class', 'node');\n\n  // Append text label WITHIN the node group\n  node.append('text').attr('class', 'node-label') // Add class for potential styling\n  .text(function (d) {\n    return d.id.length > 22 ? d.id.substring(0, 19) + '...' : d.id;\n  }) // Truncate label on chart\n  .attr('dy', function (d) {\n    return -(nodeSize(d.count) + 8);\n  }) // Adjust vertical position\n  // *** UPDATED LABEL STYLE ***\n  .style('text-anchor', 'middle').style('font-size', '12px') // Increased size\n  .style('font-weight', 'bold') // Added boldness\n  .style('fill', '#333') // Slightly darker fill\n  .style('pointer-events', 'none');\n\n  // Append circle WITHIN the node group (drawn on top of text)\n  node.append('circle').attr('r', function (d) {\n    return nodeSize(d.count);\n  }).attr('fill', function (d) {\n    return color(d.group);\n  }).style('stroke', '#fff').style('stroke-width', 2).style('cursor', 'pointer').on('mouseover', function (event, d) {\n    // --- Highlight logic ---\n    var connectedNodeIds = new Set([d.id]);\n    // Ensure robust check for source/target IDs\n    links.forEach(function (l) {\n      var sourceId = _typeof(l.source) === 'object' ? l.source.id : l.source;\n      var targetId = _typeof(l.target) === 'object' ? l.target.id : l.target;\n      if (sourceId === d.id) connectedNodeIds.add(targetId);else if (targetId === d.id) connectedNodeIds.add(sourceId);\n    });\n    link.filter(function (l) {\n      var sourceId = _typeof(l.source) === 'object' ? l.source.id : l.source;\n      var targetId = _typeof(l.target) === 'object' ? l.target.id : l.target;\n      return sourceId === d.id || targetId === d.id;\n    }).transition().duration(100).style('stroke-opacity', 0.9).style('stroke-width', function (l) {\n      return linkWidth(l.value) + 1;\n    });\n    link.filter(function (l) {\n      var sourceId = _typeof(l.source) === 'object' ? l.source.id : l.source;\n      var targetId = _typeof(l.target) === 'object' ? l.target.id : l.target;\n      return sourceId !== d.id && targetId !== d.id;\n    }).transition().duration(100).style('stroke-opacity', 0.1);\n    nodeGroup.selectAll('g.node').transition().duration(100).style('opacity', function (n) {\n      return connectedNodeIds.has(n.id) ? 1 : 0.2;\n    });\n    d3.select(this).transition().duration(100).attr('r', nodeSize(d.count) + 3).style('stroke', '#333');\n\n    // --- Tooltip Update ---\n    var tooltipMaxLength = 10;\n    // *** Truncate the main title (d.id) ***\n    var truncatedTitle = d.id.length > tooltipMaxLength ? d.id.substring(0, tooltipMaxLength) + '...' : d.id;\n    var tooltipHtml = \"<div style=\\\"font-weight: bold; margin-bottom: 5px; border-bottom: 1px solid #555;\\\">\".concat(truncatedTitle, \"</div>\"); // Use truncated title\n    tooltipHtml += \"<div>Articles: <span style=\\\"font-weight:bold;\\\">\".concat(d.count, \"</span></div>\");\n    // Connection list generation with truncation\n    var connections = links.filter(function (l) {\n      var sourceId = _typeof(l.source) === 'object' ? l.source.id : l.source;\n      var targetId = _typeof(l.target) === 'object' ? l.target.id : l.target;\n      return sourceId === d.id || targetId === d.id;\n    });\n    if (connections.length > 0) {\n      tooltipHtml += \"<div style=\\\"margin-top: 8px; font-weight: bold;\\\">Connections (\".concat(connections.length, \"):</div><ul style=\\\"margin: 2px 0 0 0; padding-left: 18px; list-style: disc;\\\">\");\n      connections.sort(function (a, b) {\n        return b.value - a.value;\n      }).forEach(function (conn) {\n        var sourceId = _typeof(conn.source) === 'object' ? conn.source.id : conn.source;\n        var targetId = _typeof(conn.target) === 'object' ? conn.target.id : conn.target;\n        var otherNodeId = sourceId === d.id ? targetId : sourceId;\n        var truncatedName = otherNodeId.length > tooltipMaxLength ? otherNodeId.substring(0, tooltipMaxLength) + '...' : otherNodeId;\n        tooltipHtml += \"<li style=\\\"margin-bottom: 2px;\\\">\".concat(truncatedName, \": <span style=\\\"font-weight:bold;\\\">\").concat(conn.value, \"</span></li>\");\n      });\n      tooltipHtml += \"</ul>\";\n    } else {\n      tooltipHtml += \"<div style=\\\"margin-top: 8px; font-style: italic;\\\">No connections.</div>\";\n    }\n    tooltip.html(tooltipHtml);\n    tooltip.transition().duration(200).style('opacity', 1);\n  }).on('mouseout', function (event, d) {\n    // --- Mouseout ---\n    link.transition().duration(200).style('stroke-opacity', 0.5).style('stroke-width', function (l) {\n      return linkWidth(l.value);\n    });\n    nodeGroup.selectAll('g.node').transition().duration(200).style('opacity', 1);\n    d3.select(this).transition().duration(100).attr('r', nodeSize(d.count)).style('stroke', '#fff');\n    tooltip.transition().duration(200).style('opacity', 0);\n  });\n\n  // --- Simulation Tick (Fixed Radial Layout) ---\n  simulation.on('tick', function () {\n    // Calculate target positions based on angles (relative to the translated group's 0,0)\n    var integrityGroup = allNodes.filter(function (n) {\n      return n.group === 'integrity';\n    });\n    var healthGroup = allNodes.filter(function (n) {\n      return n.group === 'health';\n    });\n    integrityGroup.forEach(function (node, i) {\n      var angleStep = Math.PI / (integrityGroup.length + 1 || 1);\n      var angle = Math.PI / 2 + (i + 1) * angleStep; // Left semi-circle (90 to 270 deg)\n      // Use the main radius variable\n      node.x = Math.cos(angle) * radius;\n      node.y = Math.sin(angle) * radius;\n    });\n    healthGroup.forEach(function (node, i) {\n      var angleStep = Math.PI / (healthGroup.length + 1 || 1);\n      var angle = -Math.PI / 2 + (i + 1) * angleStep; // Right semi-circle (-90 to 90 deg)\n      node.x = Math.cos(angle) * radius;\n      node.y = Math.sin(angle) * radius;\n    });\n\n    // Update link paths using the calculated/adjusted d.x, d.y\n    link.attr('d', function (d) {\n      // Need source/target objects for x/y coordinates\n      var sourceNode = allNodes.find(function (n) {\n        return n.id === (d.source.id || d.source);\n      });\n      var targetNode = allNodes.find(function (n) {\n        return n.id === (d.target.id || d.target);\n      });\n      if (!(sourceNode !== null && sourceNode !== void 0 && sourceNode.x) || !(targetNode !== null && targetNode !== void 0 && targetNode.x)) return null; // Check positions exist\n      var dx = targetNode.x - sourceNode.x;\n      var dy = targetNode.y - sourceNode.y;\n      var dr = Math.sqrt(dx * dx + dy * dy) * 1.5;\n      return \"M\".concat(sourceNode.x, \",\").concat(sourceNode.y, \"A\").concat(dr, \",\").concat(dr, \" 0 0,1 \").concat(targetNode.x, \",\").concat(targetNode.y);\n    });\n    // Update gradient coordinates\n    link.each(function (d) {\n      var sourceNode = allNodes.find(function (n) {\n        return n.id === (d.source.id || d.source);\n      });\n      var targetNode = allNodes.find(function (n) {\n        return n.id === (d.target.id || d.target);\n      });\n      if (!(sourceNode !== null && sourceNode !== void 0 && sourceNode.x) || !(targetNode !== null && targetNode !== void 0 && targetNode.x)) return;\n      var sourceIdClean = String(sourceNode.id).replace(/[^a-zA-Z0-9-_]/g, '_');\n      var targetIdClean = String(targetNode.id).replace(/[^a-zA-Z0-9-_]/g, '_');\n      var gradient = defs.select(\"#link-gradient-\".concat(sourceIdClean, \"-\").concat(targetIdClean));\n      if (!gradient.empty()) {\n        gradient.attr('x1', sourceNode.x).attr('y1', sourceNode.y).attr('x2', targetNode.x).attr('y2', targetNode.y);\n      }\n    });\n\n    // Update node group positions based on final d.x, d.y\n    node.attr('transform', function (d) {\n      return \"translate(\".concat(d.x || 0, \",\").concat(d.y || 0, \")\");\n    });\n  });\n\n  // --- Legend --- (Positioned relative to drawing area using width/height)\n  // Note: With the custom translate, these positions might need adjusting if they look off\n  var legendGroup = chart.select('.labels');\n  legendGroup.append('text').attr('x', -width / 1.5) // Adjusted position relative to group origin\n  .attr('y', -height / 2 - 15).text('Integrity Categories').style('font-size', '12px').style('fill', color('integrity')).style('font-weight', 'bold').attr('text-anchor', 'start'); // Align left\n  legendGroup.append('text').attr('x', width / 1.5) // Adjusted position relative to group origin\n  .attr('y', -height / 2 - 15).text('Health Categories').style('font-size', '12px').style('fill', color('health')).style('font-weight', 'bold').attr('text-anchor', 'end'); // Align right\n\n  // --- Run Simulation Briefly ---\n  simulation.alpha(0.8).restart();\n  setTimeout(function () {\n    simulation.stop();\n    console.log(\"Interconnection simulation stopped (Fixed Radial, Custom Center).\");\n    // Apply final positions one last time\n    node.attr('transform', function (d) {\n      return \"translate(\".concat(d.x || 0, \",\").concat(d.y || 0, \")\");\n    });\n    link.attr('d', function (d) {\n      var sourceNode = allNodes.find(function (n) {\n        return n.id === (d.source.id || d.source);\n      });\n      var targetNode = allNodes.find(function (n) {\n        return n.id === (d.target.id || d.target);\n      });\n      if (!(sourceNode !== null && sourceNode !== void 0 && sourceNode.x) || !(targetNode !== null && targetNode !== void 0 && targetNode.x)) return null;\n      var dx = targetNode.x - sourceNode.x;\n      var dy = targetNode.y - sourceNode.y;\n      var dr = Math.sqrt(dx * dx + dy * dy) * 1.5;\n      return \"M\".concat(sourceNode.x, \",\").concat(sourceNode.y, \"A\").concat(dr, \",\").concat(dr, \" 0 0,1 \").concat(targetNode.x, \",\").concat(targetNode.y);\n    });\n  }, 2000);\n}\n\n// Export functions\nvar _default = exports.default = {\n  initializeInterconnectionChart: initializeInterconnectionChart,\n  updateInterconnectionChart: updateInterconnectionChart\n};"},"sourceMaps":null,"error":null,"hash":"d81cc74f9022e2401d278bf7045f08f3","cacheData":{"env":{}}}